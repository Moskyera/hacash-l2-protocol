[project]
name = "hacash-agent-pay"
version = "0.2.0"
description = "Hacash Agent Pay SDK — L2 payments for AI agents"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
  "ecdsa>=0.19.0",
  "base58>=2.1.1",
  "requests>=2.31.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["hacash_agent_pay*"]
