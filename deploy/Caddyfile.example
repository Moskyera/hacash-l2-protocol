# TLS reverse proxy for hacash-l2-hub
# caddy run --config Caddyfile.example

hub.example.com {
	encode gzip
	reverse_proxy 127.0.0.1:9090
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options nosniff
	}
}
